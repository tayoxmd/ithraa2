# 📍 أين توجد النسخ الاحتياطية؟

## 🗂️ الموقع الافتراضي

جميع النسخ الاحتياطية يتم حفظها في **مجلد المستخدم الرئيسي** (`~` أو `/Users/amani/`)

### الملفات المحفوظة:

#### 1. نسخة احتياطية عادية:
```
~/ithraa-backup-[تاريخ].zip
مثال: ~/ithraa-backup-2024-11-04_17-30-00.zip
```

#### 2. نسخة احتياطية كاملة:
```
~/ithraa-full-backup-[تاريخ].zip
مثال: ~/ithraa-full-backup-2024-11-04_17-30-00.zip
```

#### 3. ملف .env:
```
~/ithraa-env-backup-[تاريخ].txt
مثال: ~/ithraa-env-backup-2024-11-04_17-30-00.txt
```

---

## 🔍 كيفية العثور على النسخ الاحتياطية

### في Terminal:

```bash
# عرض جميع النسخ الاحتياطية
ls -lh ~/ithraa-*

# عرض النسخ الاحتياطية فقط (ZIP)
ls -lh ~/ithraa-*.zip

# عرض ملفات .env المحفوظة
ls -lh ~/ithraa-env-*.txt

# عرض حسب التاريخ (الأحدث أولاً)
ls -lht ~/ithraa-* | head -10
```

### في Finder (macOS):

1. اضغط `Cmd+Shift+H` للذهاب إلى المجلد الرئيسي
2. ابحث عن ملفات تبدأ بـ `ithraa-`
3. أو استخدم البحث: `ithraa-backup` أو `ithraa-full-backup`

---

## 📦 كيفية إنشاء نسخة احتياطية الآن

إذا لم تكن لديك نسخة احتياطية، يمكنك إنشاء واحدة:

```bash
cd /Users/amani/Documents/GitHub/ithraa

# نسخة عادية (سريعة، حجم صغير)
./backup-project.sh

# نسخة كاملة (تحتوي على node_modules)
./backup-full.sh

# حفظ ملف .env
./backup-env.sh
```

بعد التشغيل، ستجد الملفات في:
- `~/ithraa-backup-[تاريخ].zip`
- `~/ithraa-env-backup-[تاريخ].txt`

---

## 💾 النسخ الاحتياطية على Git

إذا كنت تستخدم Git، جميع النسخ موجودة على GitHub:

```bash
# التحقق من آخر commit
git log --oneline -5

# عرض آخر commit مع التفاصيل
git show HEAD
```

الموقع: `https://github.com/[username]/ithraa`

---

## 🔄 نقل النسخ الاحتياطية

### إلى USB أو قرص خارجي:

```bash
# نسخ جميع النسخ الاحتياطية
cp ~/ithraa-* /Volumes/[اسم_القرص]/

# أو نسخ نسخة محددة
cp ~/ithraa-backup-2024-11-04_17-30-00.zip /Volumes/[اسم_القرص]/
```

### إلى iCloud أو Dropbox:

```bash
# نسخ إلى iCloud
cp ~/ithraa-backup-*.zip ~/Library/Mobile\ Documents/com~apple~CloudDocs/

# أو نسخ يدوياً من Finder
# اسحب الملفات إلى مجلد iCloud أو Dropbox
```

---

## 📝 ملاحظات مهمة

1. **النسخ الاحتياطية في مجلد المستخدم الرئيسي** (`~` أو `/Users/amani/`)
2. **كل نسخة تحتوي على التاريخ والوقت** في اسمها
3. **احفظ النسخ الاحتياطية في مكان آمن** (USB، iCloud، إلخ)
4. **احفظ ملف `.env` بشكل منفصل** باستخدام `./backup-env.sh`

---

## 🆘 إذا لم تجد نسخ احتياطية

إذا لم تجد أي نسخ احتياطية، قم بإنشاء واحدة الآن:

```bash
cd /Users/amani/Documents/GitHub/ithraa
./backup-project.sh
./backup-env.sh
```

ستجد الملفات في: `~/` (المجلد الرئيسي)

---

**آخر تحديث:** تم توضيح مواقع النسخ الاحتياطية

