# 💾 كيفية حفظ واسترجاع المشروع - دليل سريع

## 🚀 الطريقة السريعة (الأفضل)

### لحفظ نسخة احتياطية:

```bash
cd /Users/amani/Documents/GitHub/ithraa

# الطريقة 1: رفع إلى Git (الأفضل)
git add .
git commit -m "نسخة احتياطية"
git push

# الطريقة 2: نسخة ZIP سريعة
./backup-project.sh
```

### لاسترجاع على كمبيوتر آخر:

```bash
# الطريقة 1: من Git
git clone https://github.com/[username]/ithraa.git
cd ithraa
npm install
./start-dev.sh

# الطريقة 2: من ZIP
./restore-project.sh
```

---

## 📋 التفاصيل الكاملة

راجع ملف `BACKUP_AND_RESTORE.md` للتعليمات التفصيلية.

---

## ✅ ما الذي يتم حفظه؟

### ✅ يُحفظ:
- ✅ جميع ملفات الكود المصدري
- ✅ الإعدادات والملفات التكوينية
- ✅ الصور والموارد
- ✅ قاعدة البيانات و migrations
- ✅ معلومات Git (في النسخة الكاملة)

### ❌ لا يُحفظ:
- ❌ `node_modules` (يتم تثبيتها بـ `npm install`)
- ❌ `dist` (يتم بناؤها بـ `npm run build`)
- ❌ ملفات `.env` (استخدم `./backup-env.sh` لحفظها بشكل منفصل)

---

## 💡 نصائح مهمة

1. **استخدم Git** - أفضل طريقة للحفظ والاسترجاع
2. **احفظ بانتظام** - قبل أي تغيير كبير
3. **احفظ ملف `.env`** - استخدم `./backup-env.sh` قبل السفر
4. **اختبر الاستعادة** - تأكد أن النسخة تعمل
5. **لا ترفع `.env` إلى Git** - تم إضافته إلى `.gitignore` تلقائياً

---

**تم إنشاء:** 3 سكريبتات جاهزة للاستخدام

